Initial
1. menu 	//prints the available options
2. load 	//loads matrix from file
3. create 	//creates new matrix
4. edit 	//edits matrix
5. write 	//writes memory to file
6. show 	//prints matrices from memory
7. operate	//operations between matrices 


1. "Print available options" 

2. "Give path to import file"

3. Create (has its own menu)
	a. New matrix (done)
	b. Copy other matrix (done)
	c. Pull values from other matrix in the form of rows and columns 
 
4. Edit values of a matrix (and maybe id);

5. Write to output file

6. Show (has its own menu)
	a. requested matrix
	b. list all matrices from memory

7. Operate (has its own menu)
	a. Addition (done)
	b. Substraction (done)
	c. Multiplication (done)
	d. Transpose (done)
	e. Scalar multiplication (done)
	f. Determinant 
	g. (Row echelon form)
	h. Dot product (vector only)
	i. Cross product (vector only) 
	j. (Mixed) product (vector only)


